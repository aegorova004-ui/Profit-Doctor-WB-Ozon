# Sprint 1: Первый полезный импорт

План обновлён 14 июля 2026 года. Статус: **в работе; первый локальный WB preview-импорт и демо-запуск реализованы**.

## Цель

Дать пользователю проверяемый результат по одному формату Wildberries, не создавая ложной уверенности в точности прибыли и не отправляя чувствительный отчёт на сервер без необходимости.

## Срез 1 — WB XLSX preview

- [x] Подключить чтение XLSX в браузере без выполнения формул и макросов.
- [x] Читать десятичные суммы строками и переводить их в integer-копейки через `BigInt`.
- [x] Распознавать API-подобный набор колонок WB и версию `wb:api-financial-report:preview-2026-07`.
- [x] Обрабатывать продажи, возвраты, комиссию, логистику, хранение, штрафы, доплаты и удержания.
- [x] Агрегировать операции по SKU и сверять расчёт с `for_pay` до копейки.
- [x] Показывать локальную сводку и таблицу SKU с пометкой «оценка до себестоимости и рекламы».
- [x] Останавливать неизвестный или несошедшийся формат без вывода полной строки отчёта.
- [x] Покрыть binary fixture, граничные значения денег и React upload-сценарий автоматическими тестами.

## Срез 2 — подтверждённый формат и себестоимость

- [ ] Получить актуальную полностью анонимизированную выгрузку из кабинета WB; текущий fixture синтетический и не закрывает этот пункт.
- [ ] Сравнить заголовки, знаки и контрольные суммы с preview-контрактом; создать отдельную production-версию адаптера. Если реальный файл недоступен, использовать только синтетический fixture по официально подтверждённой схеме и сохранять статус preview, чтобы не обещать поддержку кабинетной выгрузки раньше проверки.
- [x] Добавить ручной ввод себестоимости единицы по SKU с явным источником значения и точным пересчётом в копейках.
- [x] Показать расшифровку каждой суммы и статус неполных данных.
- [x] Добавить флаг убыточного SKU после учёта себестоимости.

### Срез 2.1 — диагноз Profit Doctor

- [x] Посчитать разрыв каждого убыточного SKU до безубыточности без промежуточных float-вычислений.
- [x] Отсортировать проблемные SKU по величине убытка и показать первый приоритет.
- [x] Показать предельную себестоимость за период и на единицу с явной пометкой «до рекламы».
- [x] Выделить крупнейшую известную статью расходов и дать безопасное направление для проверки.
- [x] Не включать SKU без себестоимости в положительный запас и сохранять флаг гарантированного убытка, если удержания уже выше выручки.
- [x] Покрыть формулы unit-тестами и полный сценарий React integration-тестом.

## Срез 3 — результат и экспорт

- [x] Добавить demo report и удобный повторный запуск.
- [x] Реализовать CSV/XLSX-экспорт результата с версией формулы и источниками полей.
- [x] Добавить в XLSX отдельные листы «Сводка» и «SKU», закрепить заголовки и сохранить точные суммы через формулы из integer-копеек.
- [x] Защитить пользовательские поля CSV от spreadsheet formula injection и проверить реальный XLSX визуальным рендером.
- [ ] Проверить большой файл и мобильную таблицу; основной демо-сценарий уже покрыт React smoke-тестом.
- [x] Решить, нужна ли серверная история отчётов; в Sprint 1 файлы остаются только в браузере.

## Инфраструктура Sprint 1

- [x] Поднять тестовую PostgreSQL без Docker и системной установки.
- [x] Применить начальную миграцию через `npm run db:migrate` к локальной базе `profit_doctor`.

## Контрольный расчёт fixture

Синтетический файл содержит 4 операции и 3 SKU. Ожидаемые итоги:

- выручка — 6 840 ₽;
- известные удержания и возвраты — 3 062,50 ₽;
- остаток до себестоимости и рекламы — 3 777,50 ₽.

Итог равен сумме `for_pay` по всем операциям. Это техническая сверка, а не показатель реального магазина.

Контрольный ручной ввод 900 ₽, 500 ₽ и 1 300 ₽ за единицу даёт 3 700 ₽ общей себестоимости и 77,50 ₽ результата до рекламы. Два из трёх SKU становятся убыточными. Этот сценарий закреплён integration-тестом.

Диагноз для этого ввода показывает 269 ₽ суммарного убытка проблемных SKU без взаимозачёта, 346,50 ₽ положительного запаса одного SKU и ставит «Набор контейнеров» первым приоритетом. Его разрыв до безубыточности — 171,50 ₽, а предельная себестоимость до рекламы — 1 128,50 ₽ за период и на единицу.

## Критерий готовности Sprint 1

Актуальная анонимизированная выгрузка WB проходит подтверждённый адаптер без ручной правки; суммы сходятся с ручным расчётом; себестоимость и источники показаны явно; неполный результат называется оценкой; результат можно экспортировать.
